apply plugin: "war"

bootWar {
    mainClassName = "ms.kafkatest.KafkaTestApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
